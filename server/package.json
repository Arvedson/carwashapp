{
  "name": "carwashapp-server-mock",
  "version": "1.0.0",
  "description": "Mock server para CarWashApp usando JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 --host 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mock",
    "api",
    "json-server",
    "carwash",
    "react-native"
  ],
  "author": "CarWashApp Team",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
